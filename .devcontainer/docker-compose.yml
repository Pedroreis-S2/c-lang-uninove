version: '3.8'

services:
  c-dev:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: c-lang
    volumes:
      - ../src:/app
    working_dir: /app
    tty: true
    command: sh -c "chmod +x ./run.sh && sleep infinity"